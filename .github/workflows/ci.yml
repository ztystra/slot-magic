name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install flake8 black isort
      
      - name: Lint with flake8
        run: flake8 bot.py slot_manager.py --max-line-length=100 --ignore=E501,W503
      
      - name: Check formatting
        run: black --check bot.py slot_manager.py
      
      - name: Check imports
        run: isort --profile black --check-only bot.py slot_manager.py

  docker:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4
      
      - name: Build Docker image
        run: docker build -t slot-magic .
